# Eve
# Version 0.2.0

version: '3.6'
services:
    eve:
        #image: aleksxp/docker-eve-python
        build:
            context: ${PROJ_PATH}/apps/eve/
            dockerfile: Dockerfile
        #ports:
        #    - "5000:5000"
        volumes:
        #    - ${PROJ_PATH}/apps/zzthis/app:/usr/src/app
             - eve_code:/app
        depends_on:
            - mongo
        #command: python ./app.py
        networks:
            - backend_net
networks:
    backend_net:

# placed at last position of this doc., for easy to script to add external volume; With line 27 as empty
volumes:
  eve_code:
